name: Release
on:
  push:
    tags: ['v*']
jobs:
  continue-on-error: true

  release:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create release
        run: echo "Release ${{ github.ref }}"
