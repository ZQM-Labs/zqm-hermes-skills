# SKILL.md pointer (over-size mitigation)
zqm-fleet-management SKILL.md is over the 100k char limit, so it cannot be patched inline.
The resilient-link fabric class knowledge now lives in:
  references/zqm-garden-resilient-links.md
Load that file (skill_view name=zqm-fleet-management file_path=references/zqm-garden-resilient-links.md)
for: protocol-aware Garden link/failover design, the 2026-07-12 verified topology, the
PowerShell pitfalls (net-use exit-code capture, SYSTEM-task elevation gate, per-vendor SMB
share names), and the load-bearing correction that a Garden lacking a protocol is by-design,
not fragility. ACTION NEEDED: trim zqm-fleet-management SKILL.md below 100k (move more body
into references/) so inline pointers can be added again.
